<div class="container">
    {{#each matches}}
    <div class="result">
        <ul>
            <li>Name: {{recipeName}}</li>
            <li>Ingredients: {{ingredients}}</li>
            <li>Rating: {{rating}}</li>
            <li>Preparation Time: {{totalTimeInSeconds}}</li>
        </ul>
    </div>
    {{/each}}
</div>